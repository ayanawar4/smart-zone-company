services:
  - type: web
    name: smart-zone
    runtime: node
    buildCommand: cd backend && npm install && npm run build
    startCommand: node backend/dist/main.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
